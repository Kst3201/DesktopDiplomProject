<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/MainResourcesDictionary.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="TextBoxWithPlaceholder" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <ScrollViewer x:Name="PART_ContentHost"/>
                            <TextBlock x:Name="PART_Placeholder"
                                       Margin="{TemplateBinding Padding}"
                                       IsHitTestVisible="False"
                                       Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="{StaticResource TextPlaceholderBrush}"
                                       VerticalAlignment="Center"
                                       Visibility="Collapsed"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PART_Placeholder" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Padding" Value="2"/>
    </Style>

    <Style x:Key="PrimaryTextBoxWithPlaceholderStyle" TargetType="TextBox" BasedOn="{StaticResource TextBoxWithPlaceholder}">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Padding" Value="2"/>
    </Style>

    <Style x:Key="PrimaryPasswordBox" TargetType="PasswordBox">
        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
        <Setter Property="Padding" Value="2"/>
    </Style>

    <Style x:Key="TextBoxComponentRedactItem" TargetType="TextBox" BasedOn="{StaticResource PrimaryTextBoxWithPlaceholderStyle}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style x:Key="TextBoxUserPCRedactItem" TargetType="TextBox" BasedOn="{StaticResource PrimaryTextBoxWithPlaceholderStyle}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}"/>
    </Style>

    <Style x:Key="TextBoxUserPCReadOnlyRedactItem" TargetType="TextBox" BasedOn="{StaticResource TextBoxUserPCRedactItem}">
        <Setter Property="IsReadOnly" Value="True"/>
    </Style>

    <Style x:Key="ComboBoxRedactedItemReadOnly" TargetType="ComboBox">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeNormal}"/>
    </Style>

    <Style x:Key="ComboBoxRedactedItemEditable" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxRedactedItemReadOnly}">
        <Setter Property="IsEditable" Value="True"/>
    </Style>

    <Style x:Key="ComboBoxUserPCItemNotEditable" TargetType="ComboBox">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}"/>
    </Style>

    <Style x:Key="ComboBoxUserPCItemEditable" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxUserPCItemNotEditable}">
        <Setter Property="IsEditable" Value="True"/>
    </Style>

    <Style x:Key="ComboBoxUserPCItemReadOnlyNotEditable" TargetType="ComboBox">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}"/>
        <Setter Property="IsReadOnly" Value="True"/>
    </Style>

    <Style x:Key="ComboBoxUserPCItemReadOnlyEditable" TargetType="ComboBox" BasedOn="{StaticResource ComboBoxUserPCItemNotEditable}">
        <Setter Property="IsEditable" Value="True"/>
    </Style>
</ResourceDictionary>